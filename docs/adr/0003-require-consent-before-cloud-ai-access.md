# Require consent before cloud AI access

Writer remains local-first but an Agent Runtime may access and modify a complete Workspace and send its content to cloud AI services after Writer explains that boundary and obtains consent the first time AI is enabled for that Workspace. Consent persists for that Workspace instead of interrupting every request, but opening another Workspace requires separate consent.

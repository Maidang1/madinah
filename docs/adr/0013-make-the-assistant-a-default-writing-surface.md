# Make the Assistant a default writing surface

Writer becomes AI-first while keeping the Markdown document as its primary canvas: the Assistant sidebar is visible by default and restores the active conversation, text selections expose Quick Actions, and a keyboard shortcut focuses the Assistant immediately. This reverses the earlier decision to remove AI writing tools and treats AI interaction as a default reading and writing path rather than an optional toolkit.

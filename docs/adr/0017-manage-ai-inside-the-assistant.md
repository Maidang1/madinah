# Manage AI inside the Assistant

Writer keeps runtime discovery, compatibility diagnostics, Workspace consent, runtime selection, and conversation management inside the Assistant sidebar rather than restoring a general Preferences interface. Agent installation, authentication, and model configuration remain owned by the external runtime, keeping Writer's AI setup contextual and narrowly scoped.

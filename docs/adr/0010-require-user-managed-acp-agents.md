# Require user-managed ACP agents

Writer detects installed ACP-compatible agents and explains how to obtain missing ones, but does not silently download, install, update, or authenticate them. Users manage Agent Runtimes outside Writer, favoring explicit control over executable dependencies and credentials at the cost of additional onboarding work.

# Use a small extensible agent catalog

Writer includes detection definitions for Claude Agent ACP and Codex ACP and lets users add an Agent Registration with a local command and arguments from the Assistant. Custom registrations pass through the same ACP capability checks and store no API keys, allowing Grok and future agents to connect without adding provider-specific code or waiting for a Writer release.

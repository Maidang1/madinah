# Require a Workspace for AI

Writer does not enable the Assistant in standalone compact-file windows because they have no Workspace to own consent, conversations, agent authority, or write serialization. It offers to open the containing directory as a Workspace instead of silently granting an Agent Runtime access to the file's parent directory.

# Limit unattended agent authority to the Workspace

Writer allows an Agent Runtime to read and write freely inside the Workspace, but retains ACP permission prompts for terminal commands, external network side effects, and access outside the Workspace. Full Workspace autonomy therefore does not grant unattended control over the surrounding computer or external systems.

export {
  getFrontmatterDisplayDate,
  inferTitle,
  parseDocument,
  parseFrontmatter,
  serializeDocument,
  serializeFile,
} from "@madinah/content-core";

export type { ParsedDocument, ParsedFile, TitleSource } from "@madinah/content-core";

---
---

<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Astro 博客</title>
  </head>
  <body>
    <header>
      <h1>欢迎来到 Astro 博客</h1>
      <nav>
        <a href="/blog">查看博客文章 →</a>
      </nav>
    </header>

    <main>
      <section class="hero">
        <h2>使用 Astro 内容集合</h2>
        <p>
          这是一个使用 Astro 内容集合和 MDX 构建的博客示例。
          支持 Markdown 语法和 JSX 组件！
        </p>
        <div class="features">
          <article class="feature">
            <h3>📝 MDX 支持</h3>
            <p>在 Markdown 中使用 React、Vue、Svelte 等组件</p>
          </article>
          <article class="feature">
            <h3>🔒 类型安全</h3>
            <p>Zod 验证 frontmatter，TypeScript 强类型支持</p>
          </article>
          <article class="feature">
            <h3>🚀 性能优异</h3>
            <p>零 JS 默认输出，极快的构建速度</p>
          </article>
        </div>
      </section>
    </main>

    <style>
      header {
        padding: 3rem 2rem;
        text-align: center;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
      }

      h1 {
        margin: 0;
        font-size: 2.5rem;
      }

      nav {
        margin-top: 1.5rem;
      }

      nav a {
        display: inline-block;
        padding: 0.75rem 1.5rem;
        background: white;
        color: #667eea;
        text-decoration: none;
        border-radius: 8px;
        font-weight: 600;
        transition: transform 0.2s ease;
      }

      nav a:hover {
        transform: translateY(-2px);
      }

      main {
        max-width: 1000px;
        margin: 0 auto;
        padding: 3rem 2rem;
      }

      .hero {
        text-align: center;
      }

      .hero h2 {
        font-size: 2rem;
        color: #333;
        margin-bottom: 1rem;
      }

      .hero > p {
        font-size: 1.2rem;
        color: #666;
        margin-bottom: 3rem;
      }

      .features {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
      }

      .feature {
        padding: 2rem;
        background: #f8f9fa;
        border-radius: 12px;
        text-align: left;
      }

      .feature h3 {
        margin: 0 0 0.5rem 0;
        color: #333;
      }

      .feature p {
        margin: 0;
        color: #666;
      }
    </style>
  </body>
</html>

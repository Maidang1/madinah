---
import type { HTMLAttributes } from 'astro/types';
import { cn } from '../../utils/cn';

interface Props extends HTMLAttributes<'h6'> {
  id?: string;
}

const { class: className, id, ...props } = Astro.props;
---

<h6
  id={id}
  class={cn(
    'group my-3 text-sm font-semibold md:text-base',
    'text-text-primary',
    'leading-snug tracking-tight',
    'heading-group',
    className
  )}
  {...props}
>
  <slot />
</h6>
